---
'@rvmendoza/design-system': major
---

Remove the unused `--rvds-color-white` compatibility alias and `--rvds-radius-sm` token. Use the semantic color tokens and the actively supported radius scale instead.

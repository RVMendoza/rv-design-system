---
'@rvmendoza/design-system': major
---

Namespace all public CSS custom properties and stable BEM classes with `rvds`. This removes the previous unprefixed token API; consumers must migrate values such as `--color-action` to `--rvds-color-action`.

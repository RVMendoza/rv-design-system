---
'@rvmendoza/design-system': major
---

Add long-form typography, mixed nested lists, responsive media, and privacy-first embed components. Remove the `FormField` and `TextInput` exports so the public package can focus its initial component foundation on creator-site and blog content.
